<%= form_for ([@article, @article.comments.build]) do |f| %>
	<p>
		<br><%= f.label :commenter %>
		<%= f.text_field :commenter %>
	</p>

	<p>
		<br><%= f.label :body %>
		<%= f.text_area :body %>
	</p>

	<p>
		<%= f.submit %>
	</p>
<% end %>